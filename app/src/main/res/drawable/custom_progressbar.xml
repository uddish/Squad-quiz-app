<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/toryBlue"
                android:centerColor="@color/toryBlue"
                android:centerY="1.0"
                android:endColor="@color/toryBlue"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/midnightBlueTrans"
                    android:centerColor="@color/midnightBlueTrans"
                    android:centerY="1.0"
                    android:endColor="@color/midnightBlueTrans"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>